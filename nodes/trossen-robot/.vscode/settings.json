{
  // This node is its own uv project — use its own venv so its imports resolve
  // (effective when opened via dora-claude.code-workspace at the repo root).
  "python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python",
  "python.testing.pytestEnabled": true,
  "python.testing.pytestArgs": ["tests"]
}
